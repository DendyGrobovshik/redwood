import static app.cash.redwood.buildsupport.TargetGroup.TreehouseCommon

redwoodBuild {
  targets(TreehouseCommon)
  publishing()
}

apply plugin: 'org.jetbrains.kotlin.plugin.serialization'
apply plugin: 'io.github.tret9.zipline'

kotlin {
  sourceSets {
    commonMain {
      dependencies {
        api projects.redwoodCompose
        api projects.redwoodProtocol
        api libs.kotlinx.serialization.core
        api libs.okio
        api libs.zipline
      }
    }

    commonTest {
      dependencies {
        implementation libs.kotlin.test
        implementation libs.assertk
      }
    }
  }
}

android {
  namespace = 'app.cash.treehouse'
}
